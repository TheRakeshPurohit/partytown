---
'@qwik.dev/partytown': patch
---

✨ automatically execute partytown scripts added to the page after initialization, e.g. on client-side route transitions, without needing to dispatch a `ptupdate` event
